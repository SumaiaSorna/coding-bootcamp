const { response } = require("express");

const getAllTodos = () => {
  res.json({ message: "" });
};

const getTodoById = () => {
  res.json({ message: "" });
};

module.exports = {
  getAllTodos,
  getTodoById,
};
